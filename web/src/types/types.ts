export interface InputsProps {
  placeholder: string;
}

export interface FormLoginProps {
  title: string;
  link: string | any;
  titlelink: string;
}

export interface ButtonProps {
  text: string;
}
